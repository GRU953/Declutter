<?xml version="1.0" encoding="utf-8"?>
<!--
  SPDX-License-Identifier: Apache-2.0
  Copyright 2026 Aninda Sundar Howlader (GRU953)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Declutter has to see every package on the device, including the preinstalled
      ones the user never chose. There is no narrower way to enumerate them.
    -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!--
      moe.shizuku.manager.permission.API_V23 is deliberately NOT declared here. The
      dev.rikka.shizuku:provider library declares it in its own manifest and the merger
      brings it in; Shizuku's server only hands the binder to packages that request it, so
      it must never be stripped either.
    -->

    <application
        android:name=".DeclutterApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Declutter">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Declutter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Shizuku talks to the app through this provider. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>
</manifest>
